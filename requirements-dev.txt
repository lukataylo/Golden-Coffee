# Lean test/CI dependencies for Golden Coffee.
# The full requirements.txt pulls the heavy CV stack (ultralytics, opencv,
# supervision) needed to run the *real* camera. The backend, agent policy,
# music model, federated learning, schemas, and both eval suites all run on
# this lean core alone — every heavy import in perception/ and actuators/ is
# lazy, so CI stays fast and deterministic.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
httpx>=0.27.0
pydantic>=2.9.0
python-dotenv>=1.0.0
requests>=2.32.0
pytest>=8.0.0
