# Golden Coffee — backend hub ONLY (no torch/ultralytics/opencv).
# Use this for the Railway/Render/Fly deploy of the WebSocket hub.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
websockets>=13.0
httpx>=0.27.0
python-multipart>=0.0.9     # form parsing safety net for FastAPI
