# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets / caches
.env
.spotipy-cache
*.cache

# Models / media (keep the repo light)
*.pt
clips/*.mp4
clips/*.mov

# Node / Next.js (if dashboard is upgraded)
node_modules/
.next/
.vercel/

# OS
.DS_Store

# Eval artifacts (frames are large; keep manifest/report/judgments in git)
eval/frames/

# Runtime metrics history
data/

# Locally re-trained music-model weights (baked defaults live in music_model.py)
agent/music_weights.json

# Per-venue camera geometry (generate with `perception.run --preset` or `draw_zones`)
zones.json
*.zones.json
dashboard/scan/shots/
