# Golden Coffee — Python dependencies
# Perception
ultralytics>=8.3.0          # YOLO11
supervision>=0.25.0         # ByteTrack, zones, dwell timers, heatmaps (MIT)
opencv-python>=4.10.0
sounddevice>=0.4.6          # microphone capture for the Comfort Index Sound pillar (loudness + stress)

# Backend / realtime
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
httpx>=0.27.0               # producers POST events to the backend
pydantic>=2.9.0

# Agent
anthropic>=0.40.0           # Claude tool-use loop

# Actuators
spotipy>=2.24.0             # Spotify volume (needs Premium + active device)
broadlink>=0.18.3          # Infrared blaster (RM4): AC/heater + (optional) scent diffuser
phue>=1.1                  # Philips Hue smart lighting (comfort/ambiance)
python-miio>=0.5.12         # Xiaomi/Mijia lamp + scent diffuser over local miIO
pycryptodome>=3.20.0        # RC4/HMAC signing for the Xiaomi cloud transport (QR-session control)
qrcode[pil]>=7.4            # render the Mi-Home login QR in the terminal
requests>=2.32.0            # Telegram bot notifications
python-dotenv>=1.0.0        # load API keys from .env

# Live feed ingestion (YouTube/HLS -> OpenCV)
yt-dlp>=2024.0.0

# Federated learning bounty (optional; lazy-imported by federated/flock_model.py)
flock-sdk>=0.0.3
